# 数据库三范式
数据库范式的目的是为了解决数据冗余和更新异常
1NF：表中的字段必须是原子列
解决了数据不可拆分的问题，1NF没有解决冗余的问题，实际项目中是否需要拆看业务场景。
2NF：所有列必须直接依赖主键，不能部分依赖。
函数依赖：
A能唯一确定B，则B依赖于A。
2NF主要是针对联合主键的情况，如果有列部分依赖，就会出现数据冗余，更新时如果不全部更新则会出现数据不一致问题。
解决方法是拆分表。
3NF：非主键列必须直接依赖主键列，不能间接依赖。
解决了传递依赖的问题，也是避免了数据冗余。