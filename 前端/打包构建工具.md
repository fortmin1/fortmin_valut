# 自我提问
1. 有哪些打包构建工具，他们的区别是什么

# Babel（编译）
# TSC（编译）

Typescript Compiler，Typescript编译器，用来将单个ts文件编译成js文件，配置项都在tsconfig.json中。
## 特点
- 专注于ts编译（没有处理样式、字体...的能力），js编译，类型输出dts
- 强类型检查
- 单文件编译
## 使用场景
- 早期项目，没有复杂的编译需求
- 后端，工具库

## 缺点
- 仅仅支持ts编译
# Rollup
	parcel  
# Tsup
# Webpack
# Vite